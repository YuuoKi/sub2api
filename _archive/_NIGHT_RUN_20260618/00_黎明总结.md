# 00 · 黎明总结 · 无界双线彻夜推进（M0 + 契约修复 + C1 骨架）

> 夜间无人值守 · 2026-06-18 · Claude Code（opus-4-8）· 双仓本地分支、零 push
> 实际执行环境：Windows + Git Bash（任务书 WSL 路径 `/mnt/d/...` 实为 `D:/...`）

## 1. 三阶段状态 + 审查包路径

| 阶段 | 状态 | 审查包 | 本地提交 |
|---|---|---|---|
| **A · M0 收敛真相源** | ✅ **完成** | `_NIGHT_RUN_20260618/A_收敛真相源.md`（双仓各一份） | Sub2API `ed83030b` / QCanvas `c34d383` |
| **B · 接口契约修复** | ✅ **完成（B1/B2 必修 + B3 草案+构造，全测试证明，未真实调用）** | `_NIGHT_RUN_20260618/B_接口契约修复.md` | Sub2API `1be53de3`（首版）+ `831e9c98`（复审闭环） |
| **C · C1 骨架联调** | ✅ **骨架契约通（mock，已测）；真跨进程 e2e 阻塞于 key+DB** | `_NIGHT_RUN_20260618/C_C1骨架联调.md` | Sub2API `47cf1146` |

**分支（均本地、未 push）**
- Sub2API：`night-run/20260618-A-truth`（A）→ `night-run/20260618-B-contract`（A+B）→ `night-run/20260618-C-skeleton`（A+B+C，当前 tip）。
- QCanvas：`night-run/20260618-A-truth`（A）。

**关键成果一句话**
- A：双仓单一真相锚点就位（00/01/02/03/最新审查包），散落探索包归档，**零代码、零删除**。
- B：坐实并修掉 Seedance 请求侧 `aspect_ratio→ratio`（竖屏根因）+ 轮询窗随分辨率上调（1080p 生产真生效，复审抓出死代码已修）+ v2v 构造草案；**全程不真实付费调用**。
- C：实勘发现 **Sub2API 真实服务进程已在 :8080 运行**；QCanvas→Sub2API→mock→候选 的**字段契约每一跳测试证明对齐**；真 e2e 只差一把 key + QCanvas 自己的 DB。

## 2. 【待授权清单】（每项：为何需授权 / 授权后第一步 / 预算·风险）

### ① 真实付费调用验证 B1 竖屏（最高优先）
- **为何**：代码+测试已证明请求体发 `ratio:"9:16"`（竖屏），但「Ark 收到 `ratio:9:16` 是否真产出竖屏视频」只能用一次真实付费调用坐实（文档/测试无法替代）。
- **授权后第一步**：跑形态 B 真实冒烟，`SUB2API_SEEDANCE_SMOKE_ASPECT=9:16` + 720p/5s，核对产物宽高比真为竖屏。
- **预算·风险**：~个位数￥（720p/5s ≈ 108900 token 量级）；风险低，沿用既有三层关断 harness。

### ② 真实付费坐实 B3 v2v 字段名 + 请求侧 duration/resolution
- **为何**：v2v 的 Ark content 字段名（我用 `video_url`，由 image_url 模式推断）UNVERIFIED；请求侧 duration/resolution 字段名也仅由响应回显推断。
- **授权后第一步**：查 Ark 官方 v2v 文档；或一次真实 v2v 调用核对 content 字段名 + 回填请求侧字段名。
- **预算·风险**：~个位数￥；风险低（仍 mock 之外单条）。

### ③ 开 C1 真实 provider 门（M1 收尾）
- **为何**：C1 骨架（mock）已通；要让「一个人在浏览器真出一个竖屏视频回画布」需开真实 Seedance 门。
- **授权后第一步**：(a) 提供/授权一把 **API key**（鉴权到 :8080，铁律禁我读造凭据）；(b) 起 QCanvas hono-api 的 DB；(c) 先验 mock e2e（C 审查包 §5 的 2 条 curl），再按需开真实门（预算上限 + 失败退出）。
- **预算·风险**：mock e2e ¥0；真实门按 ① 的单条预算；风险中（真烧钱+需在场）。

### ④ 提交/合并裁量
- **为何**：四个本地提交（含公司代码）从不 push；是否合入主线、是否 push 到**私有**远端由老板裁量（origin 指开源上游，**永不 push**）。
- **授权后第一步**：老板指定目标分支/私有远端后再议；当前保持本地。

## 3. 环境阻断清单（+复现）

| 阻断 | 现象 | 复现 | 影响 | 绕行 |
|---|---|---|---|---|
| Docker daemon 未起 | `docker version`→`cannot find dockerDesktopLinuxEngine` | 任意 `docker ps` | 无法 compose 另起隔离栈 | :8080 已有运行实例，未强起；未启 Docker Desktop（避免无人值守下重操作） |
| 无原生 PG/Redis | `psql`/`redis-cli` not found | — | 服务栈在 WSL/容器内 | 用已运行的 :8080 |
| 无 API key | `/v1/video/providers`→401 `API_KEY_REQUIRED` | `curl :8080/v1/video/providers` | 无法鉴权跑真 e2e | 铁律禁读造凭据→列待授权①③ |
| QCanvas hono-api 需自己 DB | `.env` 需 `DATABASE_URL`/R2 | 起 `apps/hono-api` | 跑不起完整 QCanvas 进程 | 契约用 Go 测试在 CI 证明（无需 DB） |

> 全部按铁律「遇阻断记录+跳过、不死磕」处理；无任一阻断导致返工或卡死。

## 4. 整体位置 + 下一个动作

- **位置**：M0 ✅完成；**M1 / C1 = 当前唯一焦点，骨架（mock）契约已通、只差「真实 provider 门」**。未越格到 M2（Kling/图片/文本一根线没碰）。
- **下一个动作**：老板在场时 → 给一把 key + 起 QCanvas DB → 跑 C 审查包 §5 的 mock e2e（分钟级）确认骨架活体；再授权 ① 的真实竖屏单枪坐实 B1。**M1 之后才谈 M2。**

## 5. 逐条确认（铁律合规）

- ✅ **全程未 push**：未对任何 remote 执行 push；origin（Sub2API=开源上游 Wei-Shaw、QCanvas=YuuoKi）保持不变；新分支均本地、无 upstream。
- ✅ **未真实付费调用**：B 阶段全部用 localhost mock-Ark / in-memory；未带 `-tags=realsmoke`、未设 `SUB2API_VIDEO_REAL_SMOKE_ENABLED`、未开任何真实 provider 门。C 阶段未鉴权到 :8080、未触发任何真实任务。
- ✅ **未碰密钥**：未读取/打印/落库/落日志任何 key/token/凭据；C 阶段因「需 key」直接列待授权而非寻找凭据。
- ✅ **未部署 / 未碰生产 / 未 DB 迁移 / 未不可逆删改**：仅本地分支提交 + 文件系统归档（只移动未删除）；未起新服务、未碰用户在跑的 :8080 实例。
- ✅ **git 卫生**：每阶段独立分支、提交信息清晰；全程显式 `git add <file>`，**绝无 `git add .` / `reset --hard` / `clean` / `rebase`**；前序两处未提交改动（`.gitignore`、`deploy/docker-compose.b1-emptybrake.yml`）全程未动、未提交。
- ✅ **双家族复核**：B 阶段过了 3-lens 跨上下文对抗复审，抓出 1 MAJOR（B2 生产死代码）+ 2 minor 并全部修复闭环（Codex 跨族登录复核为老板的步骤，留待在场时）。

---
*物证以本目录各阶段审查包 + `git log` + `go test`/`go vet` 输出为准。局部 READY ≠ 产品 READY。*
