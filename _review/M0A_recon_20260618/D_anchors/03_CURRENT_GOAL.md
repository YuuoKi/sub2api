# 03 · CURRENT GOAL — Sub2API 收编主线

> `[DRAFT 待学者校准]` ｜ M0-A 只读侦察（2026-06-18）

## 路线图（当前位置 → 终点）

```
[M0-A 只读侦察] ✅ 已完成  ── 本审查包，产出收编计划 + 干净主线方案 + 五件套
        │
        ▼
[拍板] ◀── 你在这里：学者决策（收编策略 / 前端去留 / 品牌文案）
        │
        ▼
[M0-B 收编] ⛔ 待授权另写  ── 拉 wujie/trunk，ff 收编 D（或 squash 精修），跑全绿测试
        │                      红线：不 push、不开真门、先保未提交物证
        ▼
[M1 开采集口]  ── 动 ent/schema/usage_log.go（目前无 prompt/结果），按需扩采集字段
        │
        ▼
[产品化]  ── Seedance 真链路从"局部 READY"推到"产品 READY"；竖屏并主线生效；多实例
```

## M0-A 出口（本任务已交付）
1. 分支测绘总表 [A](../A_branch_map.md)（线性单链 + 超集 D + 重复分支）。
2. 逐 commit 处置 [B](../B_disposition.md)（保留/重复丢弃/可折叠/待学者）。
3. 干净主线方案 [C](../C_trunk_plan.md)（`wujie/trunk` + 基线 main + ff 收编 D 首选）。
4. G1–G9 复核 [F](../F_recheck.md)（✅×6 / ⚠️×3 / ❌×0，凭据 CLEAN）。

## 待学者拍板的三个决策（来自 B·§5）
1. **收编粒度**：方案 A（ff 全收 25，含 docs）还是方案 B（squash 精修）？→ 建议先 A 跑通再议。
2. **前端去留**：drama 引擎是否纳入 MVP？品牌文案"企业 AI 视频 API 调度中台" vs "无界互娱 API 控制台"取哪个？
3. **重复/空分支删除 + 未提交物证抢救**：M0-B 授权后做，先在主工作树/WSL 工作树 `git status` 自查。

## 验收（学者侧）
打开本审查包即可回答：① 哪些工作值得收、怎么收、收到哪条主线；② 收编哪些文件会冲突、先后顺序；③ 五件套锚点是否就位（打开就知道"在 M0、下一步拍板"）。且 `git status` 仅 `?? _review/`，证明全程零写跟踪内容。
