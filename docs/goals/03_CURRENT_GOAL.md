# 当前目标：Sub2API 广州内部版统一发布

日期：2026-07-28
分支：`codex/sub2api-release-20260728`
执行目录：`D:\sub2api-trunk\.worktrees\sub2api-release-20260728`
状态：**广州生产已部署 / 发布门禁通过 / HC 真实付费链路未授权**

## 目标

以当前线上 staff console 版本 `758f5c419` 为基线，整合：

- 账单余额与 API Key 配额并发保护；
- staff console 生命周期、开卡、一次性 Key 展示和管理员保护；
- HC-ATOM V3 视频与图片接入；
- 单一审查包、可回滚发布提交和广州服务器受控热更新。

## 硬边界

- 不读取、打印或提交密钥、token、cookie、生产连接串；
- 不触发真实付费 provider 调用；
- HC 视频和图片真实调度保持默认关闭；
- 不删除数据库卷、备份或用户资产，不新增公网端口；
- 主工作区既有脏文件不进入发布提交。

## 验收

1. 无合并标记，`git diff --check` 通过；
2. 后端定向、全量测试和构建通过；
3. 前端定向、全量测试、typecheck、lint 和生产构建通过；
4. Wire 重新生成且无未解释漂移；
5. Docker 镜像与 loopback 健康检查通过；
6. 审查包与最终提交、部署证据一致；
7. 服务器先做只读预检和备份，再更新应用；失败可回滚到 `758f5c419`。

本地代码、测试、类型、lint、生产构建、Docker 镜像、新库迁移和 loopback 健康检查均已通过。发布提交 `32832b350` 已部署到广州生产：9 个 Compose 服务运行，Sub2API 容器健康，公网 `/health` 返回 `ok`，公开版本为 `广州内部版 2026.07.28-r4`，`build_commit=32832b350`，端口仍仅映射 `127.0.0.1:8081`。迁移 `187_hc_atom_v3_video_payload.sql` 已且仅应用一次；数据库、配置和旧镜像回滚点均已校验。HC 视频和图片真实调度保持关闭，未执行真实付费调用，因此本轮只判定内部生产发布完成，不判定 HC 真实链路 READY。
