# 当前目标：Sub2API 广州内部版统一发布

日期：2026-07-28
分支：`codex/sub2api-release-20260728`
执行目录：`D:\sub2api-trunk\.worktrees\sub2api-release-20260728`
状态：**本地门禁通过 / 待镜像与服务器复核**

## 目标

以当前线上 staff console 版本 `758f5c419` 为基线，整合：

- 账单余额与 API Key 配额并发保护；
- staff console 生命周期、开卡、一次性 Key 展示和管理员保护；
- HC-ATOM V3 视频与图片接入；
- 单一审查包、可回滚发布提交和广州服务器受控热更新。

## 硬边界

- 不读取、打印或提交密钥、token、cookie、生产连接串；
- 不触发真实付费 provider 调用；
- HC 视频和图片真实调度保持默认关闭；
- 不删除数据库卷、备份或用户资产，不新增公网端口；
- 主工作区既有脏文件不进入发布提交。

## 验收

1. 无合并标记，`git diff --check` 通过；
2. 后端定向、全量测试和构建通过；
3. 前端定向、全量测试、typecheck、lint 和生产构建通过；
4. Wire 重新生成且无未解释漂移；
5. Docker 镜像与 loopback 健康检查通过；
6. 审查包与最终提交、部署证据一致；
7. 服务器先做只读预检和备份，再更新应用；失败可回滚到 `758f5c419`。

本地代码、测试、类型、lint 和生产构建门禁已通过。真实视频和图片付费链路未获单独授权，因此本轮最多判定为“内部可用”，不能判定 HC 生产 READY。
