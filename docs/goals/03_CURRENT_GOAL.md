# 当前目标：无界 API 中台公司内网 Release

日期：2026-07-18
分支：`codex/wujie-lan-release-20260719`
基线：`70d296f09764d8f578d7663d99982c17a94d07d5`
状态：**已阻塞 / 实施中（非生产 READY）**

## 目标

交付一个管理员专用 Web、公司局域网 HTTPS 唯一入口，以及供 QCanvas/内部系统持续调用大模型、图片和视频的正式 release candidate。

## 已确认的分支事实

- K3 `830581592` 与统一控制台 `70d296f09` 从共同基点大幅分叉，不是线性版本。
- K3 持有 `internal_real`、视频可靠性和图片归档能力；统一控制台持有唯一入口、管理员业务面和 Windows 交付脚本。
- 两分支迁移 174–176 同号异义，禁止整支 merge、直接复制迁移或共用数据卷。
- release 从统一控制台基线派生，K3 仅作为行为和实现参考；所有新增迁移使用统一控制台后续编号。

## 当前执行顺序

1. 修复 Windows 基线测试并锁定真相源。
2. 管理员专用 Web 与服务端路由阻断。
3. 持续图片资产和正式账号隔离。
4. 持续视频可靠性与完整 QCanvas API-Key 契约。
5. usage/价格可靠性。
6. Windows LAN HTTPS、bootstrap、备份恢复。
7. 全量测试、安全扫描、浏览器证据和单一审查包。

## 硬边界

- 不读取、打印或提交真实 provider 密钥。
- 不复用现有测试数据卷，不公开暴露，不 push，不 reset/clean/rebase。
- 真实付费验收与目标公司电脑部署需要现场密钥、网络参数和最终确认。
- 任何真实链路、资产交付、迁移、HTTPS、权限或恢复门槛失败，继续标记“已阻塞”，不更新 main。

详细执行计划：`docs/superpowers/plans/2026-07-18-wujie-lan-release.md`。
