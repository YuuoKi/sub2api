# 全页面视觉审查（2026-07-24）

范围：LAN 控制台全部可达页面，桌面 1440×900 + 移动 390×844，共 20 张初审截图（`audit/01-15、m-*`）+ 7 张修复复审截图（`audit/fix-*`），断言见 `audit/_fix-report.json`（17 条全绿）。

## 达标核对（老板二轮要求）

| 要求 | 结果 | 证据 |
|---|---|---|
| 总览填充、无大面积空白 | ✅ 空态「三步上手」引导卡；有数据密度收敛 | 02-overview |
| 视频 = 选平台→名称→分组→Key→可选接口 | ✅ 密钥库页内弹窗，即梦/Veo 3.1/快乐小马置灰「即将接入」 | 05/06 |
| 员工开卡一页：姓名/邮箱/分组/充值→双 Key | ✅ 单弹窗四字段，明文双 Key 一次 | 07/08 |
| 系统健康放到用量下 | ✅ 侧栏「用量」父级两项 | 02 |
| AI 调用记录不动 | ✅ 仅修英文周报块（见 P1-3） | 10a/10b |
| 分组只问组名、不搞复杂 | ✅ 内联建组仅组名；复杂配置留高级 | 04/08 |

## 初审发现 → 处理

P0：无。

P1（已修，复审断言通过）：
1. 高级菜单「视频通道」旧页与密钥库重复且副标「由系统固定」已成错误陈述 → 菜单入口摘除（URL 保留可达）；fix-01。
2. 员工卡列表「备注」列恒空、「工具」标签系内部 member_type 泄漏 → 删列删标签；fix-02/03。
3. 提示词采集 Tab 渲染英文机器文本（# Weekly Production Ledger…）→ 中文化为字段表，零数据整块隐藏；fix-04。

P2（已修）：
- 系统健康页头原名「运维监控」与菜单不一致 → 统一「系统健康」；fix-05。
- 员工卡展开行卡片表移动端无横向滚动 → 补 overflow-x-auto；fix-03m。
- LAN 模式订阅/公告/设置预加载每次导航重发 403 噪音 → store 层 lanAdminModeEnabled 跳过（SPA 导航 0 次重发，实测见 `_fix-console-requests.json`）。

P2（记录在案，未改）：
- 旧视频通道页 URL 直达仍可见旧文案（入口已摘，建议下一轮整页删除或改跳密钥库）。
- 日期输入占位 mm/dd/yyyy 为浏览器 en locale 行为，中文环境需自定义日期组件才能改。
- 登录页英文副标「Subscription to API Conversion Platform」、上游账号页「全部Privacy…」过滤器，属品牌/高级页 i18n 遗留。
- 刷新后首次预加载仍各发 1 次 403（发生在公共设置就绪前），消灭需推迟预载，收益低。

回归：vue-tsc/eslint 零告警；vitest 相关 123 用例全绿（含新增 lanPreloadGuards 4 例）。探针数据已清理（fix-07）。
