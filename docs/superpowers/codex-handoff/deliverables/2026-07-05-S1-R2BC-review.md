# 审查包：S1 R2-B/C — 扩样与 PR #3726 合并门禁

> 执行者：Codex  
> 完成时间：2026-07-05 18:45  
> 关联任务书：[CODEX_TASK_S1_R2BC_MERGE.md](../CODEX_TASK_S1_R2BC_MERGE.md)  
> 状态：`partial`（本地后端门禁通过；真实扩样缺授权；PR 外部门禁未过）

---

## 1. 本任务做了什么（给 Claude / 老板看）

- 从 S1-0 开始执行，先修复后端测试与当前视频契约不一致的问题：Seedance API-key 无 `trial_mode` 时走正式路径，缺 `production_authorized` 应返回 `VIDEO_PRODUCTION_NOT_AUTHORIZED`。
- 重跑后端完整门禁：`go test ./...` 通过，`golangci-lint run ./...` 返回 `0 issues`。
- 未读取 `.env`、Key、token、cookie，未触发 NB2 / Seedance 新真实付费调用。
- R2-B 图片 NB2 真实冒烟因缺少明确授权与可用生产 Key 证据，标为 `blocked`。
- R2-C 继承已有真实视频样本 task #4；新增 2 条视频与 3 条图片扩样因真实调用未授权，分别标为 `blocked` / `skip`。
- PR #3726 当前公开 GitHub 页面显示仍为 Open，CLA Assistant 的 `cla-check` failed，合并前需提交者签署 CLA 并复核 CI / conflict。
- 再次提醒：R2-A 使用过的临时 Seedance Key 应由老板在火山方舟控制台废弃或轮换；本审查包不含 Key 明文。

---

## 2. 改了哪些文件

| 文件 | 变更摘要 |
|------|----------|
| `backend/internal/server/routes/api_key_video_gateway_test.go` | 将 Seedance 无 `trial_mode` 的阻塞断言从旧的 `VIDEO_PROVIDER_DISABLED` 对齐为正式路径授权错误 `VIDEO_PRODUCTION_NOT_AUTHORIZED`。 |
| `docs/superpowers/codex-handoff/deliverables/2026-07-05-R2-closeout-summary.md` | 更新 R2-B/R2-C 当前状态，并补充 S1 后端门禁与 PR CLA 外部门禁。 |
| `docs/superpowers/codex-handoff/deliverables/2026-07-05-S1-R2BC-review.md` | 新增本轮总审查包，覆盖 S1-0 至 S1-3。 |

---

## 3. 验收结果（必须可核对）

| 验收项 | 结果 | 证据 |
|--------|------|------|
| S1-0 后端测试 | pass | `cd backend; go test ./...` exit 0；`internal/server/routes` 从失败点恢复为 ok。 |
| S1-0 后端 lint | pass | `cd backend; golangci-lint run ./...` exit 0；输出 `0 issues.` |
| S1-0 审查包齐全 | pass | `deliverables/2026-07-05-*.md` 可读；本轮新增 `2026-07-05-S1-R2BC-review.md`，且命中 `.gitignore` 反向放行规则。 |
| S1-0 git 债务：`user_member_type.go` | pass | `git ls-files backend/internal/service/user_member_type.go` 能返回该 tracked 文件。 |
| S1-0 git 债务：`docs/superpowers/` | pass | `git ls-files docs/superpowers/codex-handoff/deliverables/2026-07-05-R2-closeout-summary.md` 能返回 tracked 文件。 |
| S1-1 R2-B NB2 图片真实冒烟 | blocked | 未提供本轮明确 NB2 真实调用授权、预算、停止条件和无需读取密钥明文的调用方式；按边界不触发付费 provider。 |
| S1-2 R2-C 3 视频 + 3 图片扩样 | partial | 已有 video task #4 继续有效；新增 2 视频 blocked；3 图片因 R2-B blocked 而 skip。 |
| S1-2 已有样本对账 | pass | task #4：`5.0094 / 7.2 = 0.69575` USD；余额 $20.00000000 -> $19.30425000，见 R2-A/R2-C 审查包。 |
| S1-2 火山账单核对 | blocked | `upstream_task_id=cgt-20260705162538-bd5cl` 仍需老板在方舟控制台核对；Codex 不登录供应商控制台。 |
| S1-3 PR 状态 | fail | 公开 GitHub checks 页面显示 PR #3726 Open；`cla-check` failed，提示提交者需签 CLA。 |
| S1-3 `00_START_HERE.md` 一致性 | pass | 当前文件已指向 V5.0 current-state、PR #3726、R2-A task #4 与 S1 入口。 |
| 真实 Key 安全边界 | pass | 本轮未读取、打印或提交 `.env`、Key、token、cookie、员工密码。 |

---

## 4. 验证命令与结果

```text
git status --short
M 00_START_HERE.md
M backend/internal/server/routes/api_key_video_gateway_test.go
M docs/superpowers/codex-handoff/CODEX_START_HERE.md
M docs/superpowers/codex-handoff/deliverables/2026-07-05-R2-closeout-summary.md
?? docs/superpowers/codex-handoff/CODEX_TASK_S1_R2BC_MERGE.md
?? docs/superpowers/codex-handoff/deliverables/2026-07-05-S1-R2BC-review.md

git branch --show-current
wujie/video-capture-moat-20260702

git rev-parse --show-toplevel
D:/sub2api-trunk

cd backend
$env:GOCACHE = Join-Path $env:TEMP 'sub2api-gocache'
go test ./...
exit 0
key package evidence:
ok github.com/Wei-Shaw/sub2api/internal/server/routes 5.128s
ok github.com/Wei-Shaw/sub2api/internal/service 52.815s
ok github.com/Wei-Shaw/sub2api/migrations 0.606s

$env:GOLANGCI_LINT_CACHE = Join-Path $env:TEMP 'sub2api-golangci-cache'
golangci-lint run ./...
0 issues.
exit 0

git ls-files backend/internal/service/user_member_type.go docs/superpowers/codex-handoff/deliverables/2026-07-05-R2-closeout-summary.md
backend/internal/service/user_member_type.go
docs/superpowers/codex-handoff/deliverables/2026-07-05-R2-closeout-summary.md

git check-ignore -v docs/superpowers/codex-handoff/deliverables/2026-07-05-S1-R2BC-review.md
.gitignore:146:!docs/superpowers/codex-handoff/** docs/superpowers/codex-handoff/deliverables/2026-07-05-S1-R2BC-review.md
```

---

## 5. S1-2 对账扩样表

| # | 类型 | id | cost (CNY) | balance Δ (USD) | 一致？ |
|---|------|----|------------|-----------------|--------|
| 1 | video | 4 | 5.0094 | -0.69575 | pass（已有 R2-A 样本） |
| 2 | video | blocked | - | - | 缺少本轮真实调用授权 |
| 3 | video | blocked | - | - | 缺少本轮真实调用授权 |
| 4 | image | skip | - | - | R2-B NB2 冒烟 blocked |
| 5 | image | skip | - | - | R2-B NB2 冒烟 blocked |
| 6 | image | skip | - | - | R2-B NB2 冒烟 blocked |

---

## 6. 合并门禁

| 门禁项 | 结果 | 证据 / 处理 |
|--------|------|-------------|
| 本地后端测试 | pass | `go test ./...` exit 0。 |
| 本地后端 lint | pass | `golangci-lint run ./...` exit 0，`0 issues.` |
| PR #3726 状态 | fail | GitHub 公开页面：Open；checks 页面 `cla-check` failed。 |
| CLA | fail | CLA Assistant 提示 PR #3726 的提交者需要签 CLA。 |
| CI / conflict 完整状态 | 待复核 | 本机无 `gh` CLI，匿名 GitHub 页面只展示 CLA 检查与部分页面；合并前需用登录态 GitHub UI 或 `gh pr view 3726` 复核。 |
| 大文件 / secret | 待复核 | 本轮未运行 secret scan；没有新增真实 Key 或二进制资产。合并前建议跑 `tools/secret_scan.py --include-untracked`。 |
| `00_START_HERE.md` | pass | 当前 working tree 中已对齐 V5.0 current-state 与 S1 入口。 |

---

## 7. 给 Claude 的前端接口说明（如有）

- **新/改接口**：无。
- **请求/响应示例**：无。
- **前端建议改哪些页面**：无。本轮未改 `frontend/src/views/admin/console/`，也未要求 Claude 改前端。

---

## 8. 风险与遗留

- PR #3726 仍有外部门禁：CLA check failed，需提交者签 CLA 后再看 CI / review / conflict。
- R2-B NB2 图片真实冒烟未完成，缺少明确授权和可安全调用的生产 Key 证据。
- R2-C 扩样未补齐 3+3，只能称 `partial`；已有 task #4 仍是唯一真实视频对账样本。
- 火山方舟账单 +/-1% 需要老板登录供应商控制台核对 `cgt-20260705162538-bd5cl`。
- R2-A 临时 Seedance Key 必须废弃或轮换；Codex 不读取、不展示、不替老板登录供应商后台。
- 本轮未截图：变更为后端测试预期与 Markdown 审查包，不涉及前端渲染或浏览器用户路径。

---

## 9. 回滚方案

- 测试预期回滚：将 `api_key_video_gateway_test.go` 中该断言恢复为旧值；但这会再次与当前正式路径契约冲突。
- 文档回滚：还原 `2026-07-05-R2-closeout-summary.md`，删除本审查包；不会影响运行时代码。
- 若后续获得真实调用授权：只追加新的 R2-B/R2-C 样本证据，不需要回滚本轮 blocked/skip 记录。

---

## 10. 阻塞项（status=blocked/partial）

- 阻塞原因：缺少本轮明确 NB2 / Seedance 扩样真实付费调用授权、预算、停止条件和无需读取密钥明文的执行方式。
- 需要老板做什么：确认是否允许新增真实样本；若允许，提供安全调用边界、预算上限、停止条件，并确认 R2-A 临时 Key 已废弃或轮换。
- PR 合并前需要谁做什么：PR 提交者签 CLA；随后用 GitHub 登录态或 `gh pr view 3726` 复核 CI、review、conflict。

---

## 11. 可复制后续提示词

```text
请在 D:\sub2api-trunk 继续 S1 R2-B/C 收口。先读取 docs/superpowers/codex-handoff/deliverables/2026-07-05-S1-R2BC-review.md、R2-A/R2-C 审查包和 PR #3726 checks。不要读取 .env/key/token/cookie，不 push、不 merge、不 deploy。若老板明确授权真实付费调用，按授权预算补 1-3 条 NB2 图片与 2 条 Seedance 视频样本，并把 task/usage_log id、cost_estimate、余额前后、upstream_task_id 写回审查包；否则保持 R2-B blocked、R2-C partial/skip。
```
