# 审查包：V-1 — NB2 图片计费修正

> 执行者：Codex  
> 完成时间：2026-07-05 01:17  
> 执行目录：`D:\sub2api-trunk`  
> 关联规划：[2026-07-04-console-v2-roadmap.md](../../plans/2026-07-04-console-v2-roadmap.md)  
> 关联任务书：[CODEX_TASK_BILLING.md](../CODEX_TASK_BILLING.md)  
> 状态：`done`；当前状态：`内部可用 / 待复核`

---

## 1. 本任务做了什么（给 Claude / 老板看）

- 完成 V-1 图片计费修正：`gemini-3.1-flash-image-preview` 不再被按 NB Pro `$0.134` 兜底价计费。
- 打包价表新增 NB2 条目，官方四档价格落地：0.5K `$0.045`、1K `$0.067`、2K `$0.101`、4K `$0.151`。
- `CalculateImageCost` 支持模型 × 分辨率查表，并新增 `512/512px/512x512` 到 `0.5K` 的尺寸归一化。
- NB Pro 与未知模型继续保留旧兜底逻辑，避免影响现有图片计费路径。
- 恢复 `usage_logs.media_type` 列，并在图片生成记账时写入 `media_type='image'`。
- 模拟 NB2 1K 出图已由单测覆盖：`usage_logs.total_cost == 0.067`，`rate=1` 时 `actual_cost == 0.067`。
- 本轮未修改 `frontend/**`，未读取 `.env`、真实 Key、token 或 cookie，未做真实 provider 调用。

---

## 2. 改了哪些文件

| 文件 | 变更摘要 |
|------|----------|
| `backend/resources/model-pricing/model_prices_and_context_window.json` | 新增 `gemini-3.1-flash-image-preview` 价表条目和 `output_cost_per_image_by_size` 四档价格。 |
| `backend/internal/service/pricing_service.go` | LiteLLM 价表解析新增 `OutputCostPerImageBySize`，旧标量字段兼容不变。 |
| `backend/internal/service/billing_service.go` | 图片计费优先按模型 × 分辨率查表；NB2 官方四档兜底；保留 legacy 兜底倍率。 |
| `backend/migrations/149_usage_logs_media_type.sql` | 向后兼容恢复 `usage_logs.media_type` 列。 |
| `backend/internal/repository/usage_log_repo.go` | `usage_logs` select/insert/batch/best-effort/scan 参数链路补齐 `media_type`。 |
| `backend/internal/service/openai_gateway_service.go`、`backend/internal/service/gateway_service.go` | 图片生成使用日志写入 `MediaType=image`。 |
| `backend/internal/service/*_test.go`、`backend/internal/repository/*_test.go` | 增加 NB2 四档、价表解析、1K 验收、`media_type` 写入/扫描测试。 |
| `backend/internal/repository/migrations_schema_integration_test.go` | schema 集成断言新增 `usage_logs.media_type`。 |

未纳入本次交付：工作区既有 P0-3 后端改动与 `frontend/**` 改动保持原样，未回退、未清理、未覆盖。

---

## 3. 验收结果（必须可核对）

| 验收项 | 结果 | 证据 |
|--------|------|------|
| NB2 四档价格断言 | pass | `TestCalculateImageCost_NanoBanana2OfficialFallbackTiers` |
| NB2 无 PricingService 时不走 `$0.134` legacy 兜底 | pass | `TestCalculateImageCost_NanoBanana2OfficialFallbackTiers` |
| PricingService 保留分辨率价格 map | pass | `TestParsePricingData_PreservesImageOutputPriceBySize` |
| 模拟 NB2 1K 出图计费 `$0.067` | pass | `TestOpenAIGatewayServiceRecordUsage_ImageNB2Official1KCostAndMediaType` |
| `usage_logs.media_type='image'` 落库链路 | pass | `TestPrepareUsageLogInsert_ArgCountMatchesTypes`、`TestScanUsageLogRequestTypeAndLegacyFallback` |
| NB Pro / 未知模型不回归 | pass | 既有 `billing_service_image_test.go` 全量通过 |
| 不修改前端 | pass | 本轮新增/修改均在 backend、migrations、价表、deliverables；既有 `frontend/**` dirty 未触碰 |

---

## 4. 验证命令与结果

```text
# RED（预期失败）
$env:GOCACHE = (Join-Path (Get-Location) '.gocache')
$env:SUB2API_VIDEO_REAL_SMOKE_ENABLED='0'
go test -tags=unit ./internal/service -run 'Test(CalculateImageCost|ParsePricingData|OpenAIGatewayServiceRecordUsage_Image)' -count=1
FAIL: OutputCostPerImageBySize 字段尚不存在

go test -tags=unit ./internal/repository -run 'Test(PrepareUsageLogInsert|ScanUsageLog|BuildUsageLogBatchInsertQuery)' -count=1
FAIL: prepare/scan 尚未包含 media_type

# GREEN 目标测试
go test -tags=unit ./internal/service -run 'Test(CalculateImageCost|ParsePricingData|OpenAIGatewayServiceRecordUsage_Image)' -count=1
ok github.com/Wei-Shaw/sub2api/internal/service 3.911s

go test -tags=unit ./internal/repository -run 'Test(PrepareUsageLogInsert|ScanUsageLog|BuildUsageLogBatchInsertQuery)' -count=1
ok github.com/Wei-Shaw/sub2api/internal/repository 3.784s

# 触达包回归
go test -tags=unit ./internal/service ./internal/repository -count=1
ok github.com/Wei-Shaw/sub2api/internal/service 93.554s
ok github.com/Wei-Shaw/sub2api/internal/repository 5.526s

# 后端全量测试，显式关闭真实视频冒烟
go test ./...
PASS / exit 0

# 后端 lint
golangci-lint run ./...
0 issues / exit 0
warning: golangci-lint facts cache 写入 AppData 被拒；不影响 lint 结果。

# 格式检查
git diff --check
exit 0
warning: model_prices_and_context_window.json 下次 Git touch 可能 LF->CRLF。

# 秘密扫描
python tools\secret_scan.py --include-untracked
fail: 本机 PATH 无 python

C:\Users\浩臣移动工作站\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe tools\secret_scan.py --include-untracked
secret-scan: no high-confidence tracked-plus-untracked findings
```

前端截图：未截图。原因：V-1 无 UI 改动，且任务书明确禁止修改 `frontend/**`。

---

## 5. 给 Claude 的前端接口说明（如有）

- 本任务无新增前端接口。
- 后端现在会在图片生成使用日志中持久化 `media_type: "image"`；DTO 中该字段此前已存在，前端若展示调用记录可直接读取。
- NB2 512 档已计费可用，但 `/v1/messages` 路径的 `imageConfig.imageSize="512"` 透传属于 A-3，尚未在本 V-1 完成。

---

## 6. 风险与遗留

- 未解决问题：V-2 视频真实计费、V-3 币种/对账字段、V-4 归总统一、V-5 LLM fallback 小修尚未执行。
- 已读新任务书：`CODEX_TASK_API_CONTRACT.md`，下一步按老板新顺序从 A-3 开始。
- 风险：`model_prices_and_context_window.json` 有 Git 行尾 warning；本轮 `git diff --check` 仍为通过。
- 风险：当前工作区存在本轮外的 P0-3 后端与 `frontend/**` dirty 改动；本任务未回退、未清理、未提交。
- 回滚方案：撤回本轮 V-1 文件改动即可，包括价表 NB2 条目、`pricing_service` map 字段、`billing_service` NB2 四档逻辑、`149_usage_logs_media_type.sql`、usage log `media_type` 接线及对应测试。

---

## 7. 阻塞项（若 status=blocked）

- 无阻塞；V-1 后端验证已通过。
