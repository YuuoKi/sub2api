# 审查包：V-5 - LLM 兜底价小修

> 执行者：Codex  
> 完成时间：2026-07-05 03:22  
> 执行目录：`D:\sub2api-trunk`  
> 关联任务书：`docs/superpowers/codex-handoff/CODEX_TASK_BILLING.md`  
> 状态：`内部可用 / 待复核`

---

## 1. 本任务做了什么

- 修正 `claude-3-5-haiku` fallback 价，从 Haiku 4.5 价回到官方核实的 Haiku 3.5 价：`$0.80/$4`，cache write/read 为 `$1.00/$0.08`。
- 新增 `claude-opus-4.8` fallback，价格沿用 Opus 4.5/4.6/4.7：`$5/$25`，cache write/read 为 `$6.25/$0.50`。
- 新增 `claude-fable-5` 与 `claude-mythos-5` fallback：`$10/$50`，cache write/read 为 `$12.50/$1.00`。
- 新增 `claude-sonnet-5` fallback，按任务书要求使用 2026-09-01 起标准价：`$3/$15`，cache write/read 为 `$3.75/$0.30`；代码注释保留 2026-08-31 及以前官方价 `$2/$10`。
- 调整 fuzzy matching 顺序，确保 `fable`、`mythos`、`opus 4.8`、`sonnet 5` 命中新条目，不落入泛 Claude 或旧 Sonnet/Opus 兜底。
- 未修改 `frontend/**`，未真实调用 provider，未读取 `.env` / key / token / cookie。

---

## 2. 改了哪些文件

| 文件 | 变更摘要 |
|------|----------|
| `backend/internal/service/billing_service.go` | 更新 Claude fallback 价格表，新增 Fable 5 / Mythos 5 / Opus 4.8 / Sonnet 5，调整模糊匹配顺序。 |
| `backend/internal/service/billing_service_test.go` | 增加 V-5 精确价格断言和 family matching 断言，覆盖新增模型与 Haiku 3.5 修正。 |
| `docs/superpowers/codex-handoff/deliverables/2026-07-05-V-5-review.md` | 本审查包。 |

---

## 3. 验收结果

| 验收项 | 结果 | 证据 |
|--------|------|------|
| `claude-3-5-haiku` fallback 为 `$0.80/$4`，cache 为 `$1.00/$0.08` | pass | `TestGetModelPricing_ClaudeFallbackBillingV5Prices` |
| `claude-opus-4.8` fallback 与 Opus 4.5 同价 | pass | `TestGetModelPricing_ClaudeFallbackBillingV5Prices`、`TestGetFallbackPricing_FamilyMatching` |
| `claude-fable-5` / `claude-mythos-5` fallback 为 `$10/$50`，cache 为 `$12.50/$1.00` | pass | `TestGetModelPricing_ClaudeFallbackBillingV5Prices`、`TestGetFallbackPricing_FamilyMatching` |
| `claude-sonnet-5` 使用 2026-09-01 标准价 `$3/$15`，并保留 8/31 前官方价注释 | pass | `billing_service.go` 注释与单测断言 |
| `fable` / `mythos` 模糊匹配命中新条目 | pass | `TestGetFallbackPricing_FamilyMatching` |
| 不修改前端 | pass | `git status --short` 中未出现本轮新增的 `frontend/**` 修改；已有 frontend dirty 为任务前既存状态，未触碰。 |

---

## 4. 验证命令与结果

在 `D:\sub2api-trunk\backend` 执行：

```powershell
$env:GOCACHE = (Join-Path (Get-Location) '.gocache')
$env:SUB2API_VIDEO_REAL_SMOKE_ENABLED = '0'
go test -tags=unit ./internal/service -run 'Test(GetModelPricing_FallbackMatchesByFamily|GetFallbackPricing_FamilyMatching|GetModelPricing_ClaudeFallbackBillingV5Prices)' -count=1
```

结果：PASS，`ok github.com/Wei-Shaw/sub2api/internal/service 3.764s`。

```powershell
go test -tags=unit ./internal/service -count=1
```

结果：PASS，`ok github.com/Wei-Shaw/sub2api/internal/service 93.050s`。

```powershell
go test ./...
```

结果：PASS，全部后端包通过；关键包包括 `internal/handler 24.530s`、`internal/repository 5.192s`、`internal/service 52.936s`。

```powershell
golangci-lint run ./...
```

结果：PASS，`0 issues.`；环境仍提示 `AppData\Local\golangci-lint` cache 写入 `Access is denied`，不影响 lint 退出码。

```powershell
git diff --check
```

结果：PASS；仅提示两个既有工作区文件下次 Git touch 可能 `LF -> CRLF`：`api_key_video_task_contract_v1.json`、`model_prices_and_context_window.json`。

```powershell
& 'C:\Users\浩臣移动工作站\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' tools/secret_scan.py --include-untracked
```

结果：PASS，`secret-scan: no high-confidence tracked-plus-untracked findings`；环境仍提示全局 git ignore 权限不足。

---

## 5. 截图 / UI

未截图原因：V-5 仅后端计费 fallback 价格表与单测变更，无 UI 改动，且任务明确禁止修改 `frontend/**`。

---

## 6. 风险与遗留

- `claude-sonnet-5` 按任务书要求采用 2026-09-01 起标准价；如果业务要在 2026-08-31 及以前按 `$2/$10` 执行，需要后续引入按日期生效的版本化价表，本轮未做。
- fallback 价只在 channel / LiteLLM 价表都未命中时生效；若上游价表已有同名模型且价格不同，仍以现有 resolver 顺序优先。
- 当前工作树存在大量既有 dirty 文件，本轮只做 V-5 最小增量，未回退、未清理、未提交。

---

## 7. 回滚方案

- 回滚 `backend/internal/service/billing_service.go` 中 V-5 新增和修正的 Claude fallback 条目，以及 `getFallbackPricing` 对 `fable` / `mythos` / `opus 4.8` / `sonnet 5` 的分支。
- 回滚 `backend/internal/service/billing_service_test.go` 中新增的 V-5 断言和对应 family matching case。
- 删除本审查包 `docs/superpowers/codex-handoff/deliverables/2026-07-05-V-5-review.md`。

---

## 8. 给 Claude 的说明

- 本轮没有改 API 契约、数据库迁移或前端字段，只修正后端 fallback 价格。
- Claude 3.5 Haiku 当前 fallback 已是 `$0.80/$4`，不再按 Haiku 4.5 的 `$1/$5` 计费。
- Fable 5 / Mythos 5 / Opus 4.8 / Sonnet 5 已有兜底条目；`fable`、`mythos` 字样会先于泛 Claude 规则命中。
- 本轮未真实调用 provider，未读取 `.env` / key / token / cookie，未修改 `frontend/**`。
