# S3 Browser Verify Checklist

> 仓库：`D:\sub2api-trunk`  
> 分支：`wujie/video-capture-moat-20260702`  
> 日期：2026-07-07 Asia/Shanghai  
> 状态：`内部可用 / 待人工浏览器复核`  
> 禁止项：未 push，未 deploy，未读取 `.env`/token/cookie，未触发真实付费 provider。

## 1. Codex 预检记录

| 项目 | 结果 | 证据 / 备注 |
|---|---|---|
| 本机登录页 | pass | `curl.exe -sS -I --max-time 5 http://127.0.0.1:18081/login` 返回 `HTTP/1.1 200 OK` |
| 本机 health 探测 | fail | `curl.exe -sS -I --max-time 5 http://127.0.0.1:18081/health` 返回 `HTTP/1.1 404 Not Found`；该路径可能不是当前前端静态服务健康检查入口 |
| 是否启动新 dev server | skip | 未启动。任务书允许不启动 dev server；当前已有 18081 登录页可达 |
| 是否完成登录态浏览器走查 | skip | 未提供管理后台登录凭据；按边界不读取 `.env`、密钥、cookie 或账号信息 |
| 是否截图 | skip | 未完成登录态走查，不伪造截图路径 |

## 2. 人工浏览器验收表

| # | 步骤 | 结果 | 备注 |
|---|---|---|---|
| 1 | 登录管理后台（demo 模式六导航） | skip | 待人工执行；Codex 未持有登录凭据 |
| 2 | 设置页把 `usd_cny_rate` 改为 `7.5` 并保存 | skip | 待人工执行；不读取或猜测后台账号 |
| 3 | 打开 **总览**，确认总花费数字按 7.5 口径变化 | skip | 待人工执行 |
| 4 | 打开 **成员与开卡**，确认今日/累计花费为 `¥` | skip | 待人工执行 |
| 5 | 打开 **任务记录 -> AI 调用记录**，确认金额为 `¥` | skip | 待人工执行 |
| 6 | 打开 **视频任务**，确认 Seedance 行显示 `¥` 原价且 mock USD 行按 7.5 换算 | skip | 待人工执行 |
| 7 | 打开 **生成内容/提示词采集**，确认 CNY 样本如 `¥5.01` 不是 `¥37.57` | skip | 待人工执行 |
| 8 | 打开 **Groups**，确认用量列为 `¥`，订阅限额仍明确标注 USD 并显示约合人民币 | skip | 待人工执行；G2-1 已新增组件回归测试覆盖该格式 |
| 9 | 确认顶栏/个人余额仍为 USD 或明确标注 | skip | 待人工执行 |

## 3. 人工记录区

人工走查后请在此处补充实际结果、浏览器、账号类型和截图路径。截图路径必须来自真实文件，不得填写占位路径。

| # | 人工结果 | 证据 / 截图 / 备注 |
|---|---|---|
| 1 |  |  |
| 2 |  |  |
| 3 |  |  |
| 4 |  |  |
| 5 |  |  |
| 6 |  |  |
| 7 |  |  |
| 8 |  |  |
| 9 |  |  |

## 4. 当前结论

Codex 已产出可执行浏览器验收表，并确认本机登录页可达。由于没有登录凭据且禁止读取 `.env`/密钥/cookie，本轮不能宣称浏览器验收全 pass；当前状态保持为 `内部可用 / 待人工浏览器复核`。
