# 审查包：S3-G2 — S3 收口与浏览器验收表

> 执行者：Codex  
> 完成时间：2026-07-07 01:56 +08:00  
> 任务书：[CODEX_TASK_S3_G2_CLOSEOUT.md](../CODEX_TASK_S3_G2_CLOSEOUT.md)  
> 状态：`内部可用 / 待人工浏览器复核`  
> 禁止项遵守：未 push，未 deploy，未读取 `.env`/token/cookie，未触发真实付费 provider。

---

## 1. 本任务做了什么

- G2-0：已将 S3 loop 任务书和 Codex 入口更新入库，提交 `12c9dda1 docs(s3): add S3 loop task book and update codex entry`。
- G2-1：已修复 Groups 页用量列 `$` 漏网；分组今日/累计用量现在按 dashboard `usd_cny_rate` 展示为 `¥`。
- G2-1：订阅分组 `daily_limit_usd` / `weekly_limit_usd` / `monthly_limit_usd` 仍保留 USD 主语义，并显示约合人民币，避免把输入/余额语义误改。
- G2-1：新增 `GroupsView.spec.ts`，覆盖 `usd_cny_rate=7.5` 下用量列为人民币、限额仍标 USD。
- P1 affiliate 审查：`AdminAffiliateRecordsTable.vue` 的 `$` 字段来自返佣 quota / balance 链路，`pay_amount` 已是 `¥`；未改，符合“余额、quota、充值金额不要误改”边界。
- G2-2：已产出人工浏览器验收表 `2026-07-07-S3-BROWSER-VERIFY.md`；本机登录页可达，但无登录凭据，未伪造截图或 pass。
- G2-3：后端、前端全量门禁均已复跑通过。

---

## 2. 改了哪些文件

| 文件 | 变更摘要 |
|------|----------|
| `docs/superpowers/codex-handoff/CODEX_START_HERE.md` | G2-0 入库时指向 S3/G2 任务入口；之后出现外部 MLA 入口改动，本轮未纳入后续提交 |
| `docs/superpowers/codex-handoff/CODEX_TASK_S3_LOOP_CNY.md` | S3 loop 任务书入库 |
| `frontend/src/views/admin/GroupsView.vue` | Groups 用量列按 `usd_cny_rate` 展示 `¥`；订阅限额显示 `USD ...` 和 `≈¥...` |
| `frontend/src/views/admin/__tests__/GroupsView.spec.ts` | 新增 Groups 人民币用量与 USD 限额回归测试 |
| `docs/superpowers/codex-handoff/CODEX_TASK_S3_G2_CLOSEOUT.md` | G2 closeout 任务书入库 |
| `docs/superpowers/codex-handoff/deliverables/2026-07-07-S3-BROWSER-VERIFY.md` | 浏览器人工验收表与 Codex 预检记录 |
| `docs/superpowers/codex-handoff/deliverables/2026-07-07-S3-G2-closeout.md` | 本审查包 |

---

## 3. 验收结果

| 验收项 | 结果 | 证据 |
|--------|------|------|
| G2-0 文档 commit | pass | `12c9dda1 docs(s3): add S3 loop task book and update codex entry` |
| G2-1 Groups 用量列人民币展示 | pass | `npx.cmd vitest run src/views/admin/__tests__/GroupsView.spec.ts --reporter=basic`：1 file / 1 test pass |
| G2-1 Groups 限额 USD 语义保留 | pass | 新测试断言 `USD 10` 与 `≈¥75.00` 同时存在 |
| G2-1 affiliate `$` 审查 | pass | 代码审查确认 `$` 位于返佣 quota / balance；`pay_amount` 已显示 `¥`，未误改 |
| G2-2 浏览器验收表 | pass | 已生成 `2026-07-07-S3-BROWSER-VERIFY.md` |
| G2-2 本机登录页可达 | pass | `curl.exe -sS -I --max-time 5 http://127.0.0.1:18081/login` 返回 `HTTP/1.1 200 OK` |
| G2-2 登录态浏览器走查 | skip | 未提供管理后台登录凭据；禁止读取 `.env`、cookie 或账号密钥 |
| G2-3 后端测试 | pass | `go test ./...` exit 0 |
| G2-3 后端 lint | pass | `golangci-lint run ./...` exit 0，`0 issues.` |
| G2-3 前端 lint | pass | `npx.cmd eslint . --ext .ts,.vue --max-warnings=0` exit 0 |
| G2-3 前端 typecheck | pass | `npx.cmd vue-tsc --noEmit` exit 0 |
| G2-3 前端测试 | pass | `npx.cmd vitest run --reporter=basic`：106 files / 611 tests pass |

---

## 4. 验证命令与结果

```text
cd D:\sub2api-trunk\frontend
npx.cmd vitest run src/views/admin/__tests__/GroupsView.spec.ts --reporter=basic
=> PASS: 1 file / 1 test

npx.cmd eslint src/views/admin/GroupsView.vue src/views/admin/__tests__/GroupsView.spec.ts --max-warnings=0
=> PASS

npx.cmd vue-tsc --noEmit
=> PASS

cd D:\sub2api-trunk\backend
$env:GOCACHE='D:\sub2api-trunk\tmp\gocache'; go test ./...
=> PASS

$env:GOCACHE='D:\sub2api-trunk\tmp\gocache'; golangci-lint run ./...
=> PASS: 0 issues.

cd D:\sub2api-trunk\frontend
npx.cmd eslint . --ext .ts,.vue --max-warnings=0
=> PASS

npx.cmd vue-tsc --noEmit
=> PASS

npx.cmd vitest run --reporter=basic
=> PASS: 106 files / 611 tests
```

说明：Vitest 全量仍有既有 stderr 噪音，包括 auth invalid-json、SettingsView router-link stub、订阅网络错误、TableLoader server error、OpsOpenAITokenStatsCard load failure；测试结果均为 PASS，非本轮新增失败。npm 输出版本升级 notice，不影响门禁结果。

---

## 5. 浏览器验收状态

浏览器验收表：`docs/superpowers/codex-handoff/deliverables/2026-07-07-S3-BROWSER-VERIFY.md`

Codex 预检：

```text
curl.exe -sS -I --max-time 5 http://127.0.0.1:18081/login
=> HTTP/1.1 200 OK

curl.exe -sS -I --max-time 5 http://127.0.0.1:18081/health
=> HTTP/1.1 404 Not Found
```

未完成登录态浏览器走查，原因是本轮没有管理后台登录凭据，且边界禁止读取 `.env`、密钥、cookie 或账号信息。因此状态不能升级为“可演示”，保持 `内部可用 / 待人工浏览器复核`。

---

## 6. 提交记录

| 阶段 | commit |
|------|--------|
| G2-0 | `12c9dda1 docs(s3): add S3 loop task book and update codex entry` |
| G2-1 | `f2c6a61d feat(console): extend CNY display to groups and affiliate usage columns` |
| G2-4 | 本审查包、G2 任务书、浏览器验收表将由 `docs(s3): add G2 closeout and browser verify checklist` 提交承载；最终 hash 以 `git log` 和 Codex 结束汇报为准 |

未 push。

---

## 7. 风险与遗留

- 浏览器登录态走查未执行，需老板或持有合法后台账号的人按 `2026-07-07-S3-BROWSER-VERIFY.md` 勾表。
- Groups 页通过 dashboard stats 拉取 `usd_cny_rate`；接口失败时沿用现有 composable 默认 `7.2`，不会阻塞 Groups 列表。
- 订阅限额仍为 USD 输入/存储语义，本轮只加旁注，不改变后端字段含义。
- 当前工作区存在非本轮外部脏状态：`docs/superpowers/codex-handoff/CODEX_START_HERE.md` 的 MLA 入口改动、`CODEX_TASK_MLA_DBUG.md`、`_review/MEGA_LOOP_AUDIT_20260707/**`；本轮不回退、不提交这些外部材料。

---

## 8. 回滚方案

- 回滚 G2-1 前端修复：`git revert f2c6a61d`。
- 回滚 G2-0 文档入库：`git revert 12c9dda1`。
- 回滚 G2-4 文档提交：对最终 `docs(s3): add G2 closeout and browser verify checklist` commit 执行 `git revert <hash>`。
- 不使用 `reset`、`clean`、`rebase`；不删除用户未跟踪文件。

---

## 9. 可复制后续提示词

```text
你是 D:\sub2api-trunk 的执行代理（Codex）。
请按 docs/superpowers/codex-handoff/deliverables/2026-07-07-S3-BROWSER-VERIFY.md 做登录态人工浏览器复核。
不要读取 .env、token、cookie 或真实 provider key；不要 push、deploy、真实付费调用。
逐项记录 pass/fail/skip，截图只填真实文件路径。
完成后更新 docs/superpowers/codex-handoff/deliverables/2026-07-07-S3-G2-closeout.md 的浏览器验收状态。
```
