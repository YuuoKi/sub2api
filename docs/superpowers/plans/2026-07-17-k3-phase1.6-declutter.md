# K3 Phase 1.6 信息熵减计划

日期:2026-07-17
分支:`codex/k3-apple-ui-experiment-20260717`
用户指令:删掉不必要的注释、文字、日期、副标题,只留直观;业务事实不动。

## 输入

两份盘点报告(见会话):报告A(壳层/仪表盘/图表/通用组件/locale)14 条安全删除缩短 + 2 条排版统一 + 1 条人话化 + 22 个零引用 i18n key;报告B(视频 6 视图/账单/404)40 条建议 + 时间精度分级专项 + 技术块折叠专项。

## 边界(与前轮一致)

只改 frontend/** 与 docs/**;不改 backend、契约、守卫、角色、flag、计费/币种、生命周期、幂等、执行模式、资产交付语义;不合并/伪造状态;交付三事实(在线链接/本地已保存/过期时间)、失败原因、真实调用确认、币种说明一字不动;不动 VideoDashboardView L327 写死的"正常"(数据真实性议题,记录另案);mock 全程;不 push/merge/deploy。

## 工作包

### WP-E 壳层+仪表盘+通用(coder)

文件:views/admin/DashboardView.vue、views/admin/ProviderBillingView.vue、views/NotFoundView.vue、router/index.ts(仅删员工仪表盘 descriptionKey)、components/charts/TokenUsageTrend.vue、components/common/ExportProgressDialog.vue、components/common/DataTable.vue(仅注释)、i18n/locales/{zh,en}/(admin/overview.ts、dashboard.ts 及涉及命名空间)。

要点(编号对应报告A/B):
- 删仪表盘内层重复标题块(A#1);shorten emptyDescription/attentionEmpty(A#3/#4);删 welcomeMessage(A#5);删 ExportProgressDialog 重复正文(A#10)。
- TokenUsageTrend:X 轴 hour→HH:00(同天)/MM-DD HH:00、day→MM-DD,tooltip 保留完整日期(A#8);DashboardView 用户趋势同(A#9);Input/Output/Cache 等硬编码英文改走已有 i18n key(A#14)。
- periodStart/End 去"(RFC3339 格式)",格式示例挪 placeholder(A#11);字段名括弧统一全角(A#12)。
- 删除零引用 key(报告A第六节清单,执行前逐 key 重新验证 0 引用,zh/en 对称)。
- 账单页:账期只显示到日(B七#1);SHA 截断 16 位+title 全值(B七#2);时区/发票号折入高级选项 details,币种字段必须保持可见(B七#5);404 页删"联系支持"死链行(B八#1)。
- 注释瘦身(A#15/#16):删设计独白式注释;DataTable 保留虚拟滚动根因注释,悬浮滚动条注释留一行。

### WP-F 视频 6 视图(coder)

文件:views/admin/video/**(6 视图 + videoUtils.ts + 对应 __tests__)。

要点(编号对应报告B):
- 删重复副标题/说明句(VideoDashboard L20/L8、L44 简化为"不会调用真实生成服务,结果可在任务记录查看。"、L296;VideoCreate L8 与 L135 去重、L22/L53 前半句/L76/L87 后半句/L268;VideoTasks L159 副文本行、L202 小字、L228 重复"打开结果"按钮、L8;VideoTaskDetail L22-24 重复按钮删其一、L325/L323 demo 重复行、L197 首句、L128 前半句、L113、L310;VideoProviders L146 死分支、L134、L38 前半句、L194;VideoSystemCheck L8 枚举)。
- 同屏数字去重(VideoDashboard L100 结论句与 L117-123 三卡:删结论句,数字只留卡片)。
- 表头去冗余:"当前状态"→"状态"、"通道名称/是否启用/当前处理中"→"通道/启用/处理中"、单元格"当前已启用"→"已启用"(改 videoUtils.providerEnabledLabel,注意创建页同函数)、"任务失败原因"→"失败原因";分隔符 `#id | 类型` 统一;`#` 前缀保留。
- 时间精度分级:videoUtils.formatDate 加 precision 参数(默认 minute);任务详情处理时间线/运行记录保留秒;列表/仪表盘/过期时间到分;local_asset_saved_at 必须走 formatDate(修未格式化 bug)。
- 空态标点统一;mock 徽章"试跑任务,不需要真实凭证"→"无需真实凭证"。
- VideoProviders 三张说明卡紧凑化为一行三列细条(不删,含引导事实);VideoTaskDetail 非 demo 任务参数平铺 dl 折入 details(demo 保持平铺)。
- 注释编号(如 <!-- 1. 系统状态 -->)删除;业务注释(如币种注释)保留。
- 测试同步更新并保持全绿;绝不改业务逻辑。

### WP-G 收口(串行)

四门禁 + git diff --check;截图(简化后的仪表盘/视频列表/详情/通道/账单);审查包 SUMMARY 增补 Phase 1.6 节 + LATEST_REVIEW_PACKAGE.html;提交 docs(review): package k3 phase 1.6 declutter;停止。

## 记录另案(本轮不动)

- VideoDashboardView L327 写死"正常"与系统检查 /health 口径不一致(数据真实性,待用户确认)。
- user/DashboardView UTC 日界偏移(日期 bug,另案)。
- BaseDialog aria-label 英文、AppSidebar 视频菜单硬编码中文 i18n 化(下一轮)。
- VideoProviders"异常类型/最近错误"两列归一后重复(需后端语义确认)。
